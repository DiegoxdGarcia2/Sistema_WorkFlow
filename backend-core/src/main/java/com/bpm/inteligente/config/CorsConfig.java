package com.bpm.inteligente.config;

/**
 * CORS is now configured in SecurityConfig.corsConfigurationSource().
 * This class is intentionally empty — kept only to avoid import errors
 * if referenced elsewhere. CORS must go through Spring Security's filter chain
 * to work correctly with JWT authentication.
 *
 * @see SecurityConfig#corsConfigurationSource()
 * @deprecated Use SecurityConfig CORS configuration instead
 */
// Intentionally empty — CORS moved to SecurityConfig
